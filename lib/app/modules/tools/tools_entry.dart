import 'package:flutter/material.dart';
import 'tools_hub_page.dart';

class ToolsEntry extends StatelessWidget {
  const ToolsEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const ToolsHubPage();
  }
}
